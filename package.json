{
  "name": "kage",
  "version": "0.1.0",
  "description": "A virtual desktop pet",
  "main": "main.js",
  "scripts": {
    "start": "electron . --no-sandbox --ozone-platform=x11 --disable-gpu-vsync 2>/dev/null",
    "start:wayland": "electron . --no-sandbox",
    "build": "electron-builder"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^42.4.0",
    "electron-builder": "^26.0.0"
  }
}
